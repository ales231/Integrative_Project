# grub.cfg — Menú GRUB para cargar el kernel con Multiboot2
# Nota: debe ser un archivo ELF (no binario plano de objcopy).

set timeout=0
set default=0

menuentry "kernel" {
    multiboot2 /boot/kernel.elf
    boot
}
